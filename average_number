from random import randint

list_of_numbers = [randint(1, 100) for _ in range(3)]
print(list_of_numbers)
result = 0
total = len(list_of_numbers)

for number in list_of_numbers:
    result += number

average_number = result / total

print(f"Average number: {round(average_number)}")
